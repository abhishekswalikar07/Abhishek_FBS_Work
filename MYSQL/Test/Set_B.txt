1. select name from customers order by name;
2. select * from products order by cost desc limit 10;
3. Select salary from employee order by salary desc limit 1,1;
4. select city, count(*) from customers group by city;
5. select dept, sum(salary),count(*) from employee where sum(salary)>200000 group by dept  ;
6. select phone_number, customer_Name from employee where phone_number like 	'%____' and customerName like '___%';
7. select * from products where product_name like '%Pro%';
8. select course,count(*) from Student where count(*)>=30 group by course ;
9. select month(date),count(*) from order group by month(date);
10. select dept, max(salary),min(salary) from employee group by dept;
11.a)Data
12.a) order by desc
13.a) asc
14.b)15.68
15. b)count()
16. select c.customer_id,c.customer_name,avg(o.total_amount) from customer c inner join orders o on c.customer_id=o.order_id group by c.customer_id,c.customer_name having count(o.customer_id)>2;
17.select p.product_id,p.product_name,count(s.sale_id) as 'sales_count' from product p inner join sales s on p.product_id=s.product_id group by p.product_id,p.product_name having count(s.sales_id)>50;
